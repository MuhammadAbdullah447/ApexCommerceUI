# Assets

This folder is reserved for local static assets — images, icons, and custom fonts —
for when the project moves away from remote placeholder image URLs.

Suggested structure once local assets are added:

- `src/assets/images/` — product photos, banners, illustrations
- `src/assets/fonts/` — custom font files (.ttf/.otf)